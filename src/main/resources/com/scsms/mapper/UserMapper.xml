<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scsms.mapper.UserMapper">

    <resultMap id="userMap" type="com.scsms.pojo.User">
        <id column="id" property="id"></id>
        <result column="username" property="username"></result>
        <result column="password" property="password"></result>
        <result column="phone_num" property="phoneNum"></result>
        <result column="email" property="email"></result>
        <result column="gender" property="gender"></result>
    </resultMap>

    <select id="selectOne" parameterType="String" resultMap="userMap">
        select * from t_user where username = #{username};
    </select>

    <insert id="register" useGeneratedKeys="true" keyProperty="id" parameterType="com.scsms.pojo.User">
        insert into t_user (username, password, phone_num, email, gender) values (#{username}, #{password}, #{phoneNum}, #{email}, #{gender});
    </insert>

</mapper>